---
import { releaseBase, version } from '../data/release';
const githubSvgPath = 'M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z';
const primaryDownloads = {
  macosArm: `${releaseBase}/Presto-${version}-macOS-arm64.dmg`,
  macosIntel: `${releaseBase}/Presto-${version}-macOS-amd64.dmg`,
  windowsX64: `${releaseBase}/Presto-${version}-windows-amd64-installer.exe`,
  windowsArm: `${releaseBase}/Presto-${version}-windows-arm64-installer.exe`,
  linuxX64: `${releaseBase}/Presto-${version}-linux-amd64.tar.gz`,
  linuxArm: `${releaseBase}/Presto-${version}-linux-arm64.tar.gz`,
};
const homebrewInstall = 'brew tap brewforge/chinese && brew install presto';
const alternateDownloads = [
  { key: 'macos-intel', zh: 'macOS Intel', en: 'macOS Intel', url: primaryDownloads.macosIntel },
  { key: 'windows-x64', zh: 'Windows x64', en: 'Windows x64', url: primaryDownloads.windowsX64 },
  { key: 'windows-arm', zh: 'Windows ARM64', en: 'Windows ARM64', url: primaryDownloads.windowsArm },
  { key: 'linux-x64', zh: 'Linux x64', en: 'Linux x64', url: primaryDownloads.linuxX64 },
  { key: 'linux-arm', zh: 'Linux ARM64', en: 'Linux ARM64', url: primaryDownloads.linuxArm },
];
---

<section class="hero">
  <!-- Liquid glass field -->
  <div class="hero-bg" aria-hidden="true">
    <div class="liquid-plane liquid-plane-1"></div>
    <div class="liquid-plane liquid-plane-2"></div>
  </div>

  <div class="container hero-content">
    <div class="hero-badge hero-anim" style="animation-delay: 0s">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
      <span class="zh">开源免费 · MIT 协议</span>
      <span class="en">Open Source · MIT License</span>
    </div>

    <div class="hero-copy-grid">
      <h1 class="hero-title hero-anim" style="animation-delay: 0.08s">
        <span class="title-stack zh">
          <span class="title-line">Markdown</span>
          <span class="title-pair"><span class="title-connector">到</span><span class="title-line">PDF</span></span>
          <span class="title-subline gradient-text">专业排版</span>
        </span>
        <span class="title-stack en">
          <span class="title-line">Markdown</span>
          <span class="title-pair"><span class="title-connector">to</span><span class="title-line">PDF</span></span>
          <span class="title-subline gradient-text">Reimagined</span>
        </span>
      </h1>

      <div class="hero-side hero-anim" style="animation-delay: 0.18s">
        <p class="hero-desc">
          <span class="zh">基于 Typst 排版引擎，把写作、模板、预览和导出收进一个清爽的桌面工作台。</span>
          <span class="en">Powered by Typst, Presto brings writing, templates, preview, and export into one focused desktop workspace.</span>
        </p>

        <div class="hero-ctas">
          <a
            href={primaryDownloads.macosArm}
            class="btn btn-primary"
            id="hero-download-link"
            data-macos-arm={primaryDownloads.macosArm}
            data-macos-intel={primaryDownloads.macosIntel}
            data-windows-x64={primaryDownloads.windowsX64}
            data-windows-arm={primaryDownloads.windowsArm}
            data-linux-x64={primaryDownloads.linuxX64}
            data-linux-arm={primaryDownloads.linuxArm}
          >
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
            <span class="zh hero-download-label">免费下载</span>
            <span class="en hero-download-label">Download Free</span>
          </a>
          <a href="https://github.com/Presto-io/Presto" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d={githubSvgPath}/></svg>
            GitHub
          </a>
        </div>

        <div class="homebrew-card" id="homebrew-card" aria-hidden="true">
          <div class="homebrew-card-head">
            <span class="zh">推荐：Homebrew 安装</span>
            <span class="en">Recommended: Homebrew</span>
            <button class="copy-btn homebrew-copy" data-copy={homebrewInstall}>
              <span class="zh">复制</span>
              <span class="en">Copy</span>
            </button>
          </div>
          <code>{homebrewInstall}</code>
        </div>

        <p class="alternate-downloads">
          <span class="zh">其他版本：</span>
          <span class="en">Other versions:</span>
          {alternateDownloads.map((item, index) => (
            <span class="alternate-download-item" data-download-key={item.key}>
              {index > 0 && <span class="download-separator">·</span>}
              <a href={item.url} target="_blank" rel="noopener noreferrer">
                <span class="zh">{item.zh}</span>
                <span class="en">{item.en}</span>
              </a>
            </span>
          ))}
        </p>

        <div class="hero-version">
          <span class="zh">最新版本 v{version} · macOS / Windows / Linux</span>
          <span class="en">Latest v{version} · macOS / Windows / Linux</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Hero showcase -->
  <div class="container">
    <div class="hero-image hero-anim" style="animation-delay: 0.4s">
      <iframe
        src="/showcase/hero"
        class="hero-iframe"
        loading="eager"
        title="Presto Editor"
        sandbox="allow-scripts allow-same-origin"
      ></iframe>
    </div>
  </div>

  <div class="container hero-story" aria-label="Presto workflow">
    <div class="story-card story-card--editor animate-on-scroll">
      <div class="story-copy">
        <span class="story-eyebrow">
          <span class="zh">写作台</span>
          <span class="en">Editor</span>
        </span>
        <h2>
          <span class="zh">写得轻，排得准。</span>
          <span class="en">Write light. Typeset clean.</span>
        </h2>
      </div>
      <div class="software-illustration editor-illustration" aria-hidden="true">
        <div class="mini-window-bar"><span></span><span></span><span></span></div>
        <div class="mini-editor-lines">
          <i style="--w: 82%"></i><i style="--w: 46%"></i><i style="--w: 68%"></i><i style="--w: 58%"></i><i style="--w: 76%"></i>
        </div>
        <div class="mini-template-chip">gongwen</div>
      </div>
    </div>

    <div class="story-card story-card--preview animate-on-scroll">
      <div class="story-copy">
        <span class="story-eyebrow">
          <span class="zh">预览</span>
          <span class="en">Preview</span>
        </span>
        <h2>
          <span class="zh">预览像纸面一样即时成形。</span>
          <span class="en">Preview forms like paper in motion.</span>
        </h2>
      </div>
      <div class="software-illustration preview-illustration" aria-hidden="true">
        <div class="paper-page">
          <b></b><i></i><i></i><i></i><i></i>
        </div>
        <div class="paper-shadow"></div>
      </div>
    </div>

    <div class="story-card story-card--store animate-on-scroll">
      <div class="story-copy">
        <span class="story-eyebrow">
          <span class="zh">扩展</span>
          <span class="en">Extend</span>
        </span>
        <h2>
          <span class="zh">模板与技能，进入同一流程。</span>
          <span class="en">Templates and skills share one flow.</span>
        </h2>
      </div>
      <div class="software-illustration store-illustration" aria-hidden="true">
        <div class="store-tile"></div>
        <div class="store-tile"></div>
        <div class="store-tile wide"></div>
        <div class="store-tile"></div>
      </div>
    </div>
  </div>
</section>

<script is:inline>
(function () {
  var container = document.querySelector('.hero-image');
  var iframe = container ? container.querySelector('.hero-iframe') : null;
  if (!container || !iframe) return;

  function scaleHero() {
    var scale = container.offsetWidth / 1200;
    iframe.style.transform = 'scale(' + scale + ')';
    container.style.height = Math.round(800 * scale) + 'px';
  }

  scaleHero();
  window.addEventListener('resize', scaleHero);

  var cards = document.querySelectorAll('.story-card');
  for (var i = 0; i < cards.length; i++) {
    cards[i].addEventListener('pointermove', function (event) {
      var rect = this.getBoundingClientRect();
      var x = ((event.clientX - rect.left) / rect.width) * 100;
      var y = ((event.clientY - rect.top) / rect.height) * 100;
      var rx = ((event.clientY - rect.top) / rect.height - 0.5) * -13;
      var ry = ((event.clientX - rect.left) / rect.width - 0.5) * 15;
      this.style.setProperty('--rx', rx.toFixed(2) + 'deg');
      this.style.setProperty('--ry', ry.toFixed(2) + 'deg');
    });
    cards[i].addEventListener('pointerleave', function () {
      this.style.removeProperty('--rx');
      this.style.removeProperty('--ry');
    });
  }

  function inferPlatform() {
    var ua = navigator.userAgent.toLowerCase();
    var platform = navigator.platform ? navigator.platform.toLowerCase() : '';
    var isArm = /arm|aarch64/.test(ua + ' ' + platform);
    if (ua.indexOf('win') !== -1 || platform.indexOf('win') !== -1) return { platform: 'windows', key: isArm ? 'windows-arm' : 'windows-x64' };
    if (ua.indexOf('linux') !== -1 || platform.indexOf('linux') !== -1) return { platform: 'linux', key: isArm ? 'linux-arm' : 'linux-x64' };
    return { platform: 'macos', key: 'macos-arm' };
  }

  var heroDownload = document.getElementById('hero-download-link');
  var detected = inferPlatform();
  if (heroDownload) {
    var key = detected.key;
    var attr = {
      'macos-arm': 'macosArm',
      'macos-intel': 'macosIntel',
      'windows-x64': 'windowsX64',
      'windows-arm': 'windowsArm',
      'linux-x64': 'linuxX64',
      'linux-arm': 'linuxArm'
    }[key];
    var href = attr ? heroDownload.dataset[attr] : heroDownload.dataset.macosArm;
    if (href) {
      heroDownload.href = href;
    }
    var labels = heroDownload.querySelectorAll('.hero-download-label');
    var zh = key.indexOf('windows') === 0 ? '下载 Windows' : key.indexOf('linux') === 0 ? '下载 Linux' : '下载 macOS';
    var en = key.indexOf('windows') === 0 ? 'Download Windows' : key.indexOf('linux') === 0 ? 'Download Linux' : 'Download macOS';
    for (var j = 0; j < labels.length; j++) {
      labels[j].textContent = labels[j].classList.contains('zh') ? zh : en;
    }
  }

  var homebrewCard = document.getElementById('homebrew-card');
  if (homebrewCard && detected.platform === 'macos') {
    homebrewCard.classList.add('is-visible');
    homebrewCard.setAttribute('aria-hidden', 'false');
  }

  var currentAlternate = document.querySelector('[data-download-key="' + detected.key + '"]');
  if (currentAlternate) currentAlternate.hidden = true;
})();
</script>

<style>
  .hero {
    position: relative;
    padding-top: calc(var(--header-height) + 2rem);
    padding-bottom: 5rem;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
    background:
      linear-gradient(115deg, rgba(37, 99, 235, 0.12), transparent 34%),
      linear-gradient(245deg, rgba(20, 184, 166, 0.12), transparent 38%),
      linear-gradient(180deg, transparent 0%, var(--glass-wash) 100%);
  }

  .hero-bg::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255,255,255,0.32) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.24) 1px, transparent 1px);
    background-size: 72px 72px;
    mask-image: linear-gradient(180deg, black, transparent 62%);
    opacity: 0.32;
  }

  .liquid-plane {
    position: absolute;
    left: 50%;
    width: min(86vw, 1040px);
    border: 1px solid var(--glass-border);
    background: var(--glass-bg);
    box-shadow: var(--glass-shadow-soft), inset 0 1px 0 var(--glass-highlight);
    backdrop-filter: blur(26px) saturate(180%);
    -webkit-backdrop-filter: blur(26px) saturate(180%);
    transform: translateX(-50%) rotate(-2deg);
  }

  .liquid-plane-1 {
    top: 8rem;
    height: 16rem;
    border-radius: 36px;
    opacity: 0.58;
  }

  .liquid-plane-2 {
    top: 16rem;
    height: 8rem;
    width: min(70vw, 780px);
    border-radius: 999px;
    opacity: 0.44;
    transform: translateX(-50%) rotate(2deg);
  }

  /* Hero entrance animation (CSS only, no observer needed) */
  .hero-anim {
    opacity: 0;
    animation: fade-in-up 0.7s ease forwards;
  }

  /* Content */
  .hero-content {
    position: relative;
    z-index: 1;
    padding-top: 2.5rem;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.375rem 1rem;
    border-radius: 999px;
    border: 1px solid var(--glass-border);
    background: var(--glass-bg);
    box-shadow: var(--glass-shadow-soft), inset 0 1px 0 var(--glass-highlight);
    backdrop-filter: blur(18px) saturate(180%);
    -webkit-backdrop-filter: blur(18px) saturate(180%);
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--color-text-secondary);
    margin-bottom: 1.5rem;
    margin-left: clamp(0rem, 4vw, 4rem);
    transition:
      border-color 200ms ease,
      box-shadow 200ms ease,
      transform 240ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .hero-badge:hover {
    border-color: var(--color-accent);
    box-shadow: 0 0 16px rgba(59, 130, 246, 0.15);
    transform: translateY(-1px);
  }

  .hero-badge svg {
    color: var(--color-accent);
  }

  .hero-copy-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, 360px);
    gap: clamp(2rem, 6vw, 5rem);
    align-items: end;
  }

  .hero-title {
    margin: 0;
    color: var(--color-text);
  }

  .title-stack {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: clamp(0.1rem, 1vw, 0.45rem);
  }

  .title-line {
    display: block;
    font-size: clamp(4rem, 14vw, 10rem);
    font-weight: 880;
    line-height: 0.82;
    letter-spacing: 0;
  }

  .title-pair {
    display: flex;
    align-items: center;
    gap: clamp(0.7rem, 2vw, 1.4rem);
  }

  .title-connector {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: clamp(3.2rem, 8vw, 6.25rem);
    height: clamp(3.2rem, 8vw, 6.25rem);
    padding: 0 clamp(0.75rem, 2vw, 1.2rem);
    border-radius: 24px;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow-soft), inset 0 1px 0 var(--glass-highlight);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    color: var(--color-accent);
    font-size: clamp(1.9rem, 5vw, 4.7rem);
    font-weight: 820;
    line-height: 1;
    transform: rotate(-6deg);
  }

  .title-subline {
    display: block;
    font-size: clamp(2.2rem, 8vw, 6.2rem);
    font-weight: 840;
    line-height: 0.95;
  }

  .hero-side {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    padding: 1.4rem;
    margin-bottom: 0.6rem;
    border-radius: 24px;
    border: 1px solid var(--glass-border);
    background: var(--glass-bg);
    box-shadow: var(--glass-shadow-soft), inset 0 1px 0 var(--glass-highlight);
    backdrop-filter: blur(24px) saturate(175%);
    -webkit-backdrop-filter: blur(24px) saturate(175%);
  }

  .hero-desc {
    font-size: clamp(1rem, 2.5vw, 1.1875rem);
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin: 0 0 1.4rem;
  }

  .hero-ctas {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 1rem;
    flex-wrap: wrap;
    margin-bottom: 1rem;
  }

  .hero-version {
    font-size: 0.8125rem;
    color: var(--color-text-muted);
  }

  .homebrew-card {
    width: 100%;
    margin: 0 0 0.75rem;
    padding: 0.75rem;
    border-radius: 16px;
    border: 1px solid var(--glass-border);
    background:
      var(--material-sheen),
      rgba(255, 255, 255, 0.32);
    box-shadow: inset 0 1px 0 var(--glass-highlight), 0 12px 30px rgba(15, 23, 42, 0.07);
    display: none;
  }

  .homebrew-card.is-visible {
    display: block;
  }

  .homebrew-card-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    margin-bottom: 0.55rem;
    color: var(--color-accent);
    font-size: 0.78rem;
    font-weight: 800;
  }

  .homebrew-card code {
    display: block;
    padding: 0.68rem 0.75rem;
    border-radius: 10px;
    border: 1px solid var(--glass-border);
    background: var(--code-bg);
    color: var(--code-text);
    font-family: var(--font-mono);
    font-size: 0.78rem;
    line-height: 1.5;
    overflow-wrap: anywhere;
  }

  .homebrew-copy {
    flex-shrink: 0;
    padding: 0.26rem 0.58rem;
    border-radius: 999px;
    border: 1px solid var(--glass-border);
    background: var(--glass-bg-strong);
    color: var(--color-accent);
    font-size: 0.72rem;
    font-weight: 800;
    transition:
      background-color 180ms ease,
      transform 220ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .homebrew-copy:hover {
    background: var(--glass-chip-bg);
    transform: translateY(-1px);
  }

  .alternate-downloads {
    width: 100%;
    margin: -0.25rem 0 0.75rem;
    color: var(--color-text-muted);
    font-size: 0.72rem;
    line-height: 1.55;
    white-space: nowrap;
    overflow-x: auto;
    scrollbar-width: none;
  }

  .alternate-downloads::-webkit-scrollbar {
    display: none;
  }

  .alternate-downloads a {
    color: var(--color-text-muted);
    text-decoration: underline;
    text-decoration-color: transparent;
    text-underline-offset: 3px;
    transition:
      color 180ms ease,
      text-decoration-color 180ms ease;
  }

  .alternate-downloads a:hover {
    color: var(--color-accent);
    text-decoration-color: currentColor;
  }

  .download-separator {
    margin: 0 0.35rem;
    color: var(--color-border);
  }

  .hero-image {
    position: relative;
    z-index: 1;
    max-width: 1000px;
    margin: 3.5rem auto 0;
    overflow: hidden;
    border-radius: 18px;
    border: 1px solid var(--glass-border);
    background: var(--glass-bg);
    box-shadow: var(--glass-shadow-strong), inset 0 1px 0 var(--glass-highlight);
    backdrop-filter: blur(28px) saturate(180%);
    -webkit-backdrop-filter: blur(28px) saturate(180%);
    transition: transform 0.4s ease, box-shadow 0.4s ease;
  }

  .hero-image:hover {
    transform: translateY(-4px);
    box-shadow: 0 34px 90px rgba(15, 23, 42, 0.2), inset 0 1px 0 var(--glass-highlight);
  }

  @media (prefers-color-scheme: dark) {
    .hero-image:hover {
      box-shadow: 0 34px 90px rgba(0, 0, 0, 0.52), inset 0 1px 0 var(--glass-highlight);
    }
  }

  .hero-iframe {
    width: 1200px;
    height: 800px;
    border: none;
    display: block;
    transform-origin: 0 0;
  }

  .hero-story {
    position: relative;
    z-index: 1;
    height: clamp(620px, 62vw, 780px);
    margin-top: 3.5rem;
  }

  .story-card {
    --rx: 0deg;
    --ry: 0deg;
    --card-rotate: 0deg;
    min-height: 260px;
    border-radius: var(--radius);
    border: 1px solid var(--glass-border);
    background: var(--glass-bg);
    box-shadow: var(--glass-shadow-soft), inset 0 1px 0 var(--glass-highlight);
    backdrop-filter: blur(24px) saturate(175%);
    -webkit-backdrop-filter: blur(24px) saturate(175%);
    padding: 1.5rem;
    overflow: visible;
    position: absolute;
    display: grid;
    grid-template-columns: minmax(220px, 0.86fr) minmax(220px, 1fr);
    gap: 1rem;
    align-items: center;
    transform-style: preserve-3d;
    rotate: var(--card-rotate);
    cursor: default;
    transition:
      border-color 220ms ease,
      box-shadow 260ms cubic-bezier(0.22, 1, 0.36, 1),
      transform 260ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .story-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      var(--material-sheen),
      var(--interference-sheen);
    border-radius: inherit;
    opacity: 0.68;
    pointer-events: none;
    transition: opacity 220ms ease;
  }

  .story-card::after {
    content: '';
    position: absolute;
    inset: 1px;
    border-radius: calc(var(--radius) - 1px);
    background:
      linear-gradient(180deg, rgba(255,255,255,0.24), transparent 42%);
    opacity: 0.34;
    pointer-events: none;
    transition: opacity 220ms ease;
  }

  @media (hover: hover) and (pointer: fine) {
    .story-card:hover {
      border-color: var(--glass-border-strong);
      z-index: 5;
      box-shadow: 0 28px 72px rgba(15, 23, 42, 0.18), inset 0 4px 16px rgba(255,255,255,0.44), inset 0 -28px 60px rgba(20,184,166,0.08);
      transform: perspective(800px) rotateX(var(--rx)) rotateY(var(--ry)) translateY(-18px) scale(1.045);
    }

    .story-card:hover::before {
      opacity: 0.82;
    }

    .story-card:hover::after {
      opacity: 0.48;
    }

    .story-card:hover .software-illustration {
      transform: translateZ(46px) translateY(-14px) scale(1.075) rotate(-3deg);
      box-shadow: inset 0 1px 0 var(--glass-highlight), 0 34px 82px rgba(15, 23, 42, 0.22);
    }

    .story-card:hover .mini-editor-lines i:nth-child(odd) {
      transform: translateX(34px);
    }

    .story-card:hover .mini-editor-lines i:nth-child(even) {
      transform: translateX(-22px);
    }

    .story-card:hover .mini-template-chip {
      transform: translateY(-26px) translateX(20px) rotate(-8deg);
    }

    .story-card:hover .paper-page {
      transform: translateY(-30px) rotate(-9deg) scale(1.08);
    }

    .story-card:hover .paper-shadow {
      transform: translateY(22px) scaleX(0.62);
      opacity: 0.46;
    }

    .story-card:hover .store-tile:nth-child(1) {
      transform: translate(-6px, -7px) rotate(-2deg);
    }

    .story-card:hover .store-tile:nth-child(2) {
      transform: translate(7px, -5px) rotate(2deg);
    }

    .story-card:hover .store-tile:nth-child(3) {
      transform: translateY(6px) scaleX(0.98);
    }

    .story-card:hover .store-tile:nth-child(4) {
      transform: translate(6px, 6px) rotate(-1deg);
    }

    .story-card:active {
      transform: perspective(900px) rotateX(calc(var(--rx) * 0.5)) rotateY(calc(var(--ry) * 0.5)) translateY(-2px) scale(0.996);
      box-shadow: 0 12px 30px rgba(15, 23, 42, 0.1), inset 0 6px 18px rgba(15, 23, 42, 0.08);
    }
  }

  .story-card--editor {
    --card-rotate: -3deg;
    --enter-x: -44vw;
    --enter-y: 30vh;
    --enter-rotate: -24deg;
    --inner-x: 18vw;
    --inner-y: -14vh;
    --inner-rotate: 26deg;
    left: 2%;
    top: 2%;
    width: 57%;
    min-height: 520px;
    z-index: 2;
  }

  .story-card--preview {
    --card-rotate: 4deg;
    --enter-x: 42vw;
    --enter-y: -28vh;
    --enter-rotate: 22deg;
    --inner-x: -16vw;
    --inner-y: 16vh;
    --inner-rotate: -24deg;
    right: -1%;
    top: 6%;
    width: 46%;
    min-height: 265px;
    z-index: 3;
  }

  .story-card--store {
    --card-rotate: -2deg;
    --enter-x: 34vw;
    --enter-y: 40vh;
    --enter-rotate: 18deg;
    --inner-x: -18vw;
    --inner-y: -18vh;
    --inner-rotate: -22deg;
    right: 7%;
    bottom: 4%;
    width: 43%;
    min-height: 290px;
    z-index: 1;
  }

  .story-copy,
  .software-illustration {
    position: relative;
    z-index: 1;
    transform: translateZ(12px);
  }

  .story-eyebrow {
    display: inline-flex;
    width: fit-content;
    margin-bottom: 0.65rem;
    padding: 0.25rem 0.65rem;
    border-radius: 999px;
    border: 1px solid var(--glass-border);
    background: var(--glass-chip-bg);
    color: var(--color-accent);
    font-size: 0.75rem;
    font-weight: 700;
  }

  .story-copy h2 {
    max-width: 16em;
    font-size: clamp(1.35rem, 2.4vw, 2.15rem);
    line-height: 1.14;
    font-weight: 780;
    letter-spacing: 0;
  }

  .story-card--preview .story-copy h2,
  .story-card--store .story-copy h2 {
    font-size: clamp(1.15rem, 1.8vw, 1.68rem);
    line-height: 1.16;
  }

  .software-illustration {
    --pop-x: 0px;
    --pop-y: 0px;
    min-height: 210px;
    border-radius: 22px;
    border: 1px solid var(--glass-border);
    background: rgba(255, 255, 255, 0.48);
    box-shadow: inset 0 1px 0 var(--glass-highlight), 0 18px 46px rgba(15, 23, 42, 0.1);
    overflow: visible;
    transition:
      box-shadow 260ms cubic-bezier(0.22, 1, 0.36, 1),
      transform 260ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .story-card--editor .software-illustration {
    min-height: 410px;
    margin-right: -3.6rem;
    rotate: 2deg;
  }

  .story-card--preview .software-illustration {
    margin-right: -2.4rem;
    rotate: -4deg;
  }

  .story-card--store .software-illustration {
    margin-right: -3rem;
    margin-bottom: -1.5rem;
    rotate: 3deg;
  }

  .mini-window-bar {
    display: flex;
    gap: 0.36rem;
    padding: 0.85rem;
    border-bottom: 1px solid rgba(15, 23, 42, 0.08);
    background: rgba(255,255,255,0.42);
  }

  .mini-window-bar span {
    width: 0.55rem;
    height: 0.55rem;
    border-radius: 50%;
    background: rgba(37, 99, 235, 0.35);
  }

  .mini-editor-lines {
    display: grid;
    gap: 0.8rem;
    padding: 1.4rem 1.2rem;
  }

  .mini-editor-lines i {
    display: block;
    width: var(--w);
    height: 0.8rem;
    border-radius: 999px;
    background: linear-gradient(90deg, rgba(37, 99, 235, 0.2), rgba(20, 184, 166, 0.18));
    transition: transform 260ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .mini-template-chip {
    position: absolute;
    left: 1.2rem;
    bottom: 1.2rem;
    padding: 0.5rem 0.8rem;
    border-radius: 999px;
    background: rgba(255,255,255,0.66);
    border: 1px solid var(--glass-border);
    color: var(--color-accent);
    font-size: 0.8rem;
    font-weight: 700;
    transition: transform 260ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .preview-illustration {
    display: grid;
    place-items: center;
  }

  .paper-page {
    width: 42%;
    min-width: 96px;
    aspect-ratio: 0.72;
    border-radius: 6px;
    background: rgba(255,255,255,0.86);
    box-shadow: 0 18px 40px rgba(15, 23, 42, 0.16);
    padding: 1.1rem 0.8rem;
    transition: transform 260ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .paper-page b,
  .paper-page i {
    display: block;
    margin: 0 auto 0.55rem;
    border-radius: 999px;
    background: rgba(15, 23, 42, 0.18);
  }

  .paper-page b {
    width: 58%;
    height: 0.75rem;
    margin-bottom: 1rem;
  }

  .paper-page i {
    width: 78%;
    height: 0.38rem;
  }

  .paper-shadow {
    position: absolute;
    bottom: 2.2rem;
    width: 34%;
    min-width: 88px;
    height: 16px;
    border-radius: 999px;
    background: rgba(15, 23, 42, 0.18);
    transform: translateY(0) scaleX(1);
    opacity: 0.32;
    transition:
      opacity 260ms ease,
      transform 260ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .store-illustration {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.7rem;
    padding: 1rem;
  }

  .store-tile {
    border-radius: 16px;
    background:
      linear-gradient(135deg, rgba(37, 99, 235, 0.2), rgba(20, 184, 166, 0.18)),
      rgba(255,255,255,0.54);
    border: 1px solid var(--glass-border);
    box-shadow: inset 0 1px 0 var(--glass-highlight);
    transition: transform 260ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .store-tile.wide {
    grid-column: span 2;
  }

  @keyframes story-reveal {
    from {
      opacity: 0;
      translate: var(--enter-x) var(--enter-y);
      rotate: var(--enter-rotate);
      scale: 0.62;
    }
    to {
      opacity: 1;
      translate: 0 0;
      rotate: var(--card-rotate);
      scale: 1;
    }
  }

  @keyframes story-inner-fly {
    from {
      opacity: 0;
      transform: translate3d(var(--inner-x), var(--inner-y), 0) rotate(var(--inner-rotate)) scale(0.66);
    }
    to {
      opacity: 1;
      transform: translateZ(12px);
    }
  }

  @supports (animation-timeline: view()) {
    .story-card {
      opacity: 0;
      translate: var(--enter-x) var(--enter-y);
      rotate: var(--enter-rotate);
      scale: 0.62;
      animation: story-reveal 900ms cubic-bezier(0.16, 1, 0.3, 1) both;
      animation-timeline: view();
      animation-range: entry 0% cover 36%;
    }

    .story-card .software-illustration,
    .story-card .story-copy {
      opacity: 0;
      animation: story-inner-fly 950ms cubic-bezier(0.16, 1, 0.3, 1) both;
      animation-timeline: view();
      animation-range: entry 6% cover 40%;
    }

    .story-card--preview {
      animation-range: entry 4% cover 32%;
    }

    .story-card--store {
      animation-range: entry 8% cover 36%;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding-top: calc(var(--header-height-mobile) + 1.5rem);
    }

    .hero-content {
      padding-top: 1.5rem;
    }

    .hero-badge {
      margin-left: 0;
    }

    .hero-copy-grid {
      grid-template-columns: 1fr;
      gap: 1.3rem;
    }

    .title-line {
      font-size: clamp(3.2rem, 17vw, 4.6rem);
    }

    .title-subline {
      font-size: clamp(2.1rem, 12vw, 3.35rem);
    }

    .title-connector {
      min-width: 3.2rem;
      height: 3.2rem;
      border-radius: 16px;
    }

    .hero-desc br {
      display: none;
    }

    .liquid-plane-1 {
      top: 7rem;
      height: 12rem;
    }

    .liquid-plane-2 {
      top: 14rem;
      height: 6rem;
      width: 82vw;
    }

    .hero-story {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1.4rem;
      height: auto;
    }

    .story-card,
    .story-card--editor {
      position: relative;
      inset: auto;
      width: auto;
      min-height: 0;
      grid-template-columns: 1fr;
    }

    .story-card--preview,
    .story-card--store {
      position: relative;
      inset: auto;
      width: auto;
    }

    .story-card--editor .software-illustration {
      min-height: 260px;
    }

    .story-card--editor .software-illustration,
    .story-card--preview .software-illustration,
    .story-card--store .software-illustration {
      margin: 0 -0.6rem -0.4rem 0;
      rotate: 0deg;
    }
  }

  @media (prefers-color-scheme: dark) {
    .story-card:hover {
      box-shadow: 0 22px 54px rgba(0, 0, 0, 0.42), inset 0 5px 18px rgba(255,255,255,0.1), inset 0 -30px 68px rgba(125,211,252,0.06);
    }

    .software-illustration {
      background:
        var(--material-sheen),
        var(--interference-sheen),
        rgba(44, 42, 50, 0.72);
    }

    .paper-page {
      background: rgba(244, 244, 245, 0.86);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-anim,
    .story-card {
      animation: none !important;
      opacity: 1 !important;
      transform: none !important;
      translate: none !important;
      rotate: none !important;
      scale: none !important;
    }

    .story-card .software-illustration,
    .story-card .story-copy {
      animation: none !important;
      opacity: 1 !important;
    }

    .hero-image:hover {
      transform: none;
    }

    .story-card:hover,
    .story-card:active,
    .story-card:hover .software-illustration,
    .story-card:hover .mini-editor-lines i,
    .story-card:hover .mini-template-chip,
    .story-card:hover .paper-page,
    .story-card:hover .paper-shadow,
    .story-card:hover .store-tile {
      transform: none;
    }
  }
</style>
